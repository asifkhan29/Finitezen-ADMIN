// import { hrUsers, type Country } from "@/mock/data";

// const countryByEmail: Record<string, Country> = Object.fromEntries(
//   hrUsers.map((u) => [u.email, u.country])
// );
// export const getCountry = (email: string): Country | undefined => countryByEmail[email];
